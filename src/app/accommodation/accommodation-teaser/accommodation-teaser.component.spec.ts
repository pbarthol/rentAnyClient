/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AccommodationTeaserComponent } from './accommodation-teaser.component';

describe('Component: RentobjectTeaser', () => {
  it('should create an instance', () => {
    let component = new AccommodationTeaserComponent();
    expect(component).toBeTruthy();
  });
});
